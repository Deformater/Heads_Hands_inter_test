package inters;

public class Monster extends Entity{
    protected Monster(int maxHealth, int attack, int defence, int minDamage, int maxDamage) {
        super(maxHealth, attack, defence, minDamage, maxDamage);
    }
}
